{
  "name": "devops-task-app",
  "version": "1.0.0",
  "description": "Simple Express app for DevOps Round 1/2 tasks (Docker + ECR + Terraform + CI/CD)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
